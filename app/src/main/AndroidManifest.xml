<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendence">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Attendence">
        <activity android:name=".forget_teacher"></activity>
        <activity android:name=".phn_teacher" />
        <activity android:name=".phn_student" />
        <activity android:name=".signupteacher" />
        <activity android:name=".loginteacher" />
        <activity android:name=".signupstudent" />
        <activity android:name=".loginstudent" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>